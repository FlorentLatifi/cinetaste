# Development and CI only — the runtime image installs requirements.txt.
-r requirements.txt

pytest==9.1.1  # >=9.0.3 fixes PYSEC-2026-1845 (/tmp/pytest-of-USER on UNIX)
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.16.8
